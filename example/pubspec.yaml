name: nepali_transliteration_example
description: "Demonstrates how to use the nepali_transliteration package."
publish_to: 'none'

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  nepali_transliteration:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
